<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joo.scribblesonthebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".SplashActivity" />
        <activity android:name=".LoginActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".WritingScribbleActivity" />
        <activity android:name=".DetailBookInfoActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".RegistProfileActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".FindPassword1Activity" />
        <activity android:name=".FindPassword2Activity" />
        <activity android:name=".FindPassword3Activity"></activity>
    </application>

</manifest>
